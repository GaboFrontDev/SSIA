{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build jade classes and run",
      "type": "shell",
      "command": ["cd ./CLASE_2/codigo/jade && run.bat"],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
